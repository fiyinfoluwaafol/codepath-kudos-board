{
  "dependencies": {
    "@prisma/client": "^5.15.1",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.3",
    "prisma": "^5.15.1"
  },
  "scripts": {
    "build": "prisma migrate deploy && prisma generate",
    "devserver": "./node_modules/nodemon/bin/nodemon.js index.js",
    "start": "node index.js"
  }
}
